# CodeIgniter 4 한국어 번역 가이드

본 가이드는 CodeIgniter 4 사용자 가이드 한국어 번역 프로젝트의 기술 표준 및 규칙을 정의합니다.

## 1. reST(reStructuredText) 마크업 규칙
Sphinx 빌드 시 발생하는 `Inline strong/literal start-string without end-string` 경고를 방지하고, HTML 출력물을 자연스럽게 유지하기 위한 규칙입니다.

### 마크업과 조사 결합 규칙
마크업(`**`, ` `` `, `:doc:`, 등) 뒤에 한국어 조사(~은, ~는, ~이, ~가, ~를 등)가 올 경우, **백슬래시와 공백(`\ `)**을 사용하여 파서에게 마크업의 끝을 알려줍니다.

- **잘못된 예:** `**강조**조사` (파싱 에러/경고 발생)
- **잘못된 예:** `**강조** 조사` (HTML 출력물에 원치 않는 공백 발생)
- **올바른 예:** `**강조**\ 조사` (HTML에서는 붙어서 출력되지만 파서 경고는 방지)

#### 주요 패턴
| 마크업 유형 | 예시 코드 | 출력 결과 |
| :--- | :--- | :--- |
| **굵게(Strong)** | `**모델**\ 은` | **모델**은 |
| **코드(Literal)** | `\`addRedirect()\`\ 에서` | `addRedirect()`에서 |
| **링크(Doc)** | `:doc:\`페이지\`\ 를` | 페이지를 |
| **강조(Emphasis)** | `*중요*\ 함` | *중요*함 |

## 2. 작업 워크플로우
1. **PO 파일 생성/업데이트:** `sphinx-intl update -p build/gettext -l ko`
2. **번역:** `msgstr` 영역에 한국어 번역 입력.
3. **마크업 검수:** 번역 완료 후 마크업 기호 뒤에 조사가 붙어있는지 확인.
4. **빌드 검증:** `sphinx-build -M html source build -D language='ko'` 실행.
5. **빌드 경고 수정:** 경고 발생 시 `fix_rst_korean_backslash.py` 스크립트 활용 또는 수동 수정.
6. **커밋:** `git commit` 및 `git push origin develop`

## 3. 원칙
- **스타일 유지:** 문법 경고를 피하기 위해 코드 스타일(` ` `)이나 강조(`**`) 자체를 삭제하지 않습니다.
- **가독성:** 기술 문서의 일관성을 위해 메서드명, 클래스명, 경로 등은 항상 코드 스타일(` ` `)로 표기합니다.
- **안전성:** 항상 작업을 커밋 단위로 나누어 수행하며, 문제가 생기면 즉시 `git checkout .`으로 롤백합니다.
